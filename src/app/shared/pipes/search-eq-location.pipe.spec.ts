import { SearchEqLocationPipe } from './search-eq-location.pipe';

describe('SearchEqLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEqLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
